[project]
name = "talesbot"
version = "1.0.0"
description = "Discord bot for Tales from the Sprawl"
authors = [{ name = "Carl Malmgren", email = "callem217@gmail.com" }]
dependencies = []
readme = "README.md"
requires-python = ">= 3.8"

[project.scripts]
"talesbot" = "talesbot:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/talesbot"]

#[tool.poetry.dependencies]
#python = "^3.11"
#discord-py-interactions = {extras = ["jurigged"], version = "^5.10.0"}
#python-dotenv = "^1.0.0"
#peewee = "^3.16.3"
#pydantic = "^2.2.1"
#pydantic-settings = "^2.0.3"
